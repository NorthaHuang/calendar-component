import styled from 'styled-components';

const DATE_BOX_SIZE = '1.75em';
export const DateBodyWrapper = styled.div`
  b,
  button {
    width: ${DATE_BOX_SIZE};
    height: ${DATE_BOX_SIZE};
    line-height: ${DATE_BOX_SIZE};
  }
`;
